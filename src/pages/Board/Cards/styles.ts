import styled from 'styled-components';

const colors = {
  primary: '#F78764',
  secondary: '#666666',
  tertiary: '#ECF0F1',
};

export const Container = styled.div`
  display: flex;
  flex-flow: row wrap;
  justify-content: space-between;
  align-items: center;
  gap: 20px;
`;

